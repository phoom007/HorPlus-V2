# Task: 20260925-tenant-b4 — สถานะ "รอตรวจสอบ" และเหตุผลที่ปฏิเสธ (Card B4)

## 1. Metadata
- **Task ID**: `20260925-tenant-b4`
- **Card**: `B4` (`docs/dev/TENANT-PRODUCTION-BACKLOG.md` §9, lines 594–608)
- **Start HEAD**: `7ce75c4`
- **Start Status (`git status --short`)**:
  - Pre-existing uncommitted files (DO NOT TOUCH / NEVER COMMIT):
    - `docker-compose.windows-pilot.yml`
    - `package.json`
    - `scripts/agent/run-r1-live-browser.mjs`
    - `src/pages/tenant/tabs/TenantProfileTab.tsx`
    - `src/tests/test-candidate-tenant-render.test.tsx`
- **Risk Assessment**: Risky (`money: bills, payments; payment review status; shared tenant portal views`) -> Requires Gate A + Gate B.

## 2. PO Request & Decisions (Verbatim)
- **PO Prompt**: `"เริ่ม B4 ได้เลยครับ"` ตามด้วยการชี้แจงใน Grilling session:
  - `"จากรูปนี้ ดูเหมือนคุณเพิ่ม UI ใหม่เข้ามา ซึ่งปุ่ม "รับเงินสด" มีอยู่แล้วคือปุ่มขวาล่าง ดังนั้น ปุ่มเขียน รับเงิน / ยืนยันการชำระ (Overide) จะไม่มี , และ b3-4-owner-override-modal.png จะไม่ให้ระบุครับ เพราะผมต้องการกดปุ่ม ขวาล่าง "รับเงินสด" และ cooldown 5 วิตามที่ออกแบบละชำระได้เลยโดยไม่ต้องบอกเหตุผลใดๆ , /grill-me"`
  - ยืนยันการทำงานของ B4: `"โอเครตามนี้เลยครับ"` (2026-09-25)
- **PO Decisions (Backlog Card B4 & Grilling Alignment)**:
  - **การจัดการแท็บ 4 "สลิปผิดพลาด" ในหน้าเจ้าของหอพัก**:
    - ถอดปุ่ม `"รับเงิน / ยืนยันการชำระ (Override)"` และ Modal บังคับกรอกเหตุผลออกทั้งหมด
    - คงปุ่มเดิมที่มีอยู่แล้วคือ `[ให้แนบใหม่]` และ `[รับเงินสด]` (พร้อม Cooldown 5 วินาที) สามารถกดชำระเงินสดเพื่อปิดยอดบิลได้ทันทีโดยไม่ต้องระบุเหตุผลใดๆ เมื่อชำระแล้วการ์ดจะแสดงป้าย `[ชำระครบแล้ว]`
  - **การจัดการแท็บ 1 "รอตรวจสลิป" ในหน้าเจ้าของหอพัก**:
    - สลิปที่ผ่านการตรวจของ SlipOK (VERIFIED) เจ้าของสามารถกดปุ่ม `[✓ ยอมรับ]` พร้อม Cooldown 5 วินาที เพื่ออนุมัติสลิปได้ทันทีโดยไม่ต้องระบุเหตุผล
    - สลิปที่ SlipOK ตรวจไม่ผ่านหรือไม่สมบูรณ์ หากต้องการอนุมัติ ยังคงต้องบันทึกเหตุผล Override ตามกฎเหล็ก `REQUIREMENTS-LOCK.md` §7
    - ปุ่ม `[✕ ปฏิเสธ]` ยังคงเปิด Modal ให้เจ้าของเลือก/ระบุเหตุผลการปฏิเสธได้ตามปกติ เพื่อส่งเหตุผลคำต่อคำให้ผู้เช่า
  - **สถานะ "รอตรวจสอบ" ในหน้าพอร์ทัลผู้เช่า**:
    - เมื่อผู้เช่าส่งสลิปแล้ว (มี payment สถานะ `UNDER_REVIEW` หรือบิลมีสลิปรอตรวจ) ป้ายสถานะบิลต้องแสดงเป็น **"รอตรวจสอบ"** (สีคราม/น้ำเงิน) ทั้งในหน้าแรก (HomeTab) และหน้าบิล (PaymentsTab) แม้ว่าสถานะหลักของบิลใน DB จะเป็น `issued`/`unpaid`
    - ป้องกันไม่ให้ผู้เช่ากดส่งสลิปซ้ำซ้อนขณะรอการตรวจสอบ
  - **การแสดงเหตุผลการปฏิเสธและปุ่มส่งใหม่**:
    - เมื่อสลิปถูกปฏิเสธ (สถานะ `REJECTED`) ผู้เช่าต้องเห็นเหตุผลที่เจ้าของระบุแบบคำต่อคำ ทั้งในกล่องแจ้งเตือนหน้าแรก (HomeTab) และในหน้ารายละเอียด/ชำระเงิน (TenantPaymentView) โดยแก้ไขการอ่านตัวแปร `rejectedReason` ให้ถูกต้อง
    - มีปุ่มให้ผู้เช่ากด **"ส่งสลิปใหม่"** เพื่อแนบสลิปใหม่เข้ามาได้ทันที
  - **การส่งสลิปใหม่ (Re-submission)**:
    - เมื่อผู้เช่าแนบและส่งสลิปใหม่สำเร็จ รายการจะถูกส่งกลับเข้าแท็บ 1 "รอตรวจสลิป" ของเจ้าของหอพัก
    - หน้าจอของผู้เช่าจะเปลี่ยนกลับมาแสดงสถานะ **"รอตรวจสอบ"**
  - **ความปลอดภัยและการแยกข้อมูล (Data Isolation)**:
    - ผู้เช่าคนอื่น (เช่น TB) ต้องไม่สามารถดูสถานะสลิปหรือเหตุผลการปฏิเสธของ TC ได้ (403 Forbidden)
  - **โหมดการทดสอบ**: ทดสอบมุมมอง D (Mobile Viewport บนเบราว์เซอร์) เป็นหลัก

## 3. Acceptance Criteria & Live Verification Evidence
| AC ID | Actor / Role | Steps & Expected Result | Mode | Result | Evidence |
|---|---|---|---|---|---|
| B4-1 | TC | ส่งสลิป → reload แล้วบิลขึ้น "รอตรวจสอบ" ทั้งหน้าแรก (HomeTab) และหน้าบิล (PaymentsTab/InvoiceView) | D, M | **PASS** | - Viewport D Home: `.agents/local/screenshots/b4-1-tenant-checking-home.png` แสดงป้าย "รอตรวจสอบ" สีคราม, แบนเนอร์ "อยู่ระหว่างรอตรวจสอบสลิป" พร้อม icon หมุน, ปุ่มชำระเงินถูก disabled<br>- Viewport D Invoice: `.agents/local/screenshots/b4-1-tenant-checking-invoice.png` แสดงป้าย "รอตรวจสอบ", icon หมุน และไม่สามารถติ๊กชำระซ้ำซ้อนได้<br>- Live API: `GET /api/v1/tenant-portal/bills` คืน `status: 'checking'` เมื่อมี payment `UNDER_REVIEW` |
| B4-2 | Owner → TC | เจ้าของปฏิเสธพร้อมเหตุผล → TC เห็นเหตุผลคำต่อคำ และปุ่มส่งใหม่ | OW, D | **PASS** | - Owner Desktop Tab 4: `.agents/local/screenshots/b4-2-owner-rejected-tab4.png` แสดงสลิปที่ถูกปฏิเสธพร้อมกล่องสีแดง `เหตุผลที่ปฏิเสธ: สลิปไม่ชัดเจน กรุณาแนบสลิปโอนเงินที่เห็นยอดและเวลาชัดเจน` พร้อมปุ่ม `[ให้แนบใหม่]` และ `[รับเงินสด]` (ไม่มีปุ่ม override ตามสั่ง)<br>- Viewport D Home: `.agents/local/screenshots/b4-2-tenant-rejected-home.png` แสดงป้าย "ปฏิเสธสลิป", กล่องสีแดงคำต่อคำ และปุ่มสีแดง "ส่งสลิปใหม่"<br>- Viewport D Payment Modal: `.agents/local/screenshots/b4-2-tenant-rejected-modal.png` แสดงกล่องสีแดงด้านบน "สลิปก่อนหน้านี้ถูกปฏิเสธ: สลิปไม่ชัดเจน..." ชัดเจน<br>- Live API: `POST /api/v1/payments/reject` สำเร็จ และ `GET /api/v1/tenant-portal/bills` ส่ง `rejectedReason` คำต่อคำ |
| B4-3 | TC | กดส่งใหม่และส่งสลิปสำเร็จ → บิลกลับเป็น "รอตรวจสอบ" | D | **PASS** | - Viewport D Home: `.agents/local/screenshots/b4-3-tenant-resubmitted-checking.png` หลังส่งสลิปใหม่ บิลกลับมาเป็นสถานะ "รอตรวจสอบ", แบนเนอร์ "อยู่ระหว่างรอตรวจสอบสลิป" และซ่อนกล่องปฏิเสธรอบก่อน<br>- Owner Tab 1: `.agents/local/screenshots/b4-3-owner-tab1-under-review.png` สลิปที่ส่งใหม่กลับเข้ามาในแท็บ 1 "รอตรวจสลิป" ของเจ้าของหอพักทันที<br>- Live API: สร้างสลิปใหม่ส่งเข้า `/payments` สำเร็จ บิลเปลี่ยนเป็น `checking` |
| B4-4 | TB | TB เรียกดูสถานะบิลหรือเหตุผลของ TC ด้วย API → 403/401 Forbidden ไม่เห็นข้อมูล | API | **PASS** | - Live API: `scripts/agent/check-b4-live.mjs` ทดสอบ session TB ยิงขอข้อมูลบิล `7cf62473-664e-438f-af8f-90330081d290` ของ TC ได้รับสถานะ 401/403 ปฏิเสธการเข้าถึง ไม่สามารถดูสลิปหรือเหตุผลการปฏิเสธของผู้อื่นได้ |

## 4. Plan & Implementation Steps
- [x] Step 1: ปรับปรุง `server/src/services/room-billing-state.service.ts` และ backend verification (F-1 & F-2):
  - ใน `room-billing-state.service.ts`: constructor รับ `prismaClient?: any` และ query รวม Payment ล่าสุด หากมีสถานะ `UNDER_REVIEW` หรือ `checking` คืน `state: 'checking_payment'`, `statusText: 'กำลังตรวจสอบการชำระเงิน'`
  - ใน `server/src/routes/tenant-portal.routes.ts`: คำนวณ `effectiveStatus` ('checking' เมื่อมี payment `UNDER_REVIEW`, 'rejected' เมื่อ payment ล่าสุดคือ `REJECTED`)
  - **คงการตรวจสอบ `OVERRIDE_REASON_REQUIRED` ใน backend `server/src/services/payment.service.ts`** สำหรับสลิปโอนเงินที่ไม่ผ่าน SlipOK (UNVERIFIED / REJECTED) ตามกฎเหล็ก `REQUIREMENTS-LOCK.md` §7 line 145
- [x] Step 2: ปรับปรุง UI เจ้าของใน `src/pages/owner/payments.tsx` (F-1):
  - ใน Tab 4 (สลิปผิดพลาด): ถอดปุ่ม `[รับเงิน / ยืนยันการชำระ (Override)]` และ Modal ระบุเหตุผล (`overrideModalTarget`) ออกทั้งหมดตามที่ PO สั่ง
  - คืนค่าปุ่มคู่เดิมใน Tab 4: `[ให้แนบใหม่]` และ `[รับเงินสด]` (พร้อม Cooldown 5s และป้าย `[ชำระครบแล้ว]` เมื่อบิลเสร็จสิ้น) โดยการกดรับเงินสดจะเรียก `POST /payments/cash` ปิดยอดบิลด้วยเงินสดได้ทันทีโดยไม่ต้องระบุเหตุผล
  - ใน Tab 1 (รอตรวจสลิป): สลิปที่ผ่าน SlipOK สามารถกดอนุมัติ (cooldown 5s) ได้ทันที ส่วนสลิปที่ต้อง Override ยังคงมี Modal บันทึกเหตุผลตามกฎ §7
- [x] Step 3: ปรับปรุง UI ผู้เช่าใน `src/pages/tenant/tabs/TenantHomeTab.tsx`, `TenantPaymentsTab.tsx`, `TenantPaymentView.tsx`, `TenantInvoiceView.tsx`, และ `TenantWorkspace.tsx`:
  - ใน `TenantHomeTab.tsx`:
    - ตรวจจับบิลที่มี payment สถานะ `UNDER_REVIEW` หรือ `checking` ให้แสดงป้ายสถานะ **"รอตรวจสอบ"** (สีคราม/indigo)
    - ป้องกันการกดส่งสลิปซ้ำซ้อนขณะรอตรวจสอบ โดยเปลี่ยนปุ่มชำระเงินเป็นปุ่ม disabled แสดงข้อความ "อยู่ระหว่างรอตรวจสอบสลิป"
    - เมื่อมีสลิปถูกปฏิเสธ (`status === 'REJECTED'`) แสดงกล่องแจ้งเตือนสีแดงพร้อมเหตุผลจริง (`rejectedReason`) และปุ่ม "ส่งสลิปใหม่"
    - เมื่อกลับมาเป็น `isChecking` ซ่อนกล่องแจ้งเตือนการปฏิเสธของรอบก่อน
  - ใน `TenantPaymentsTab.tsx`:
    - ตรวจจับบิลที่มี payment `UNDER_REVIEW` หรือ `checking` ให้แสดงป้ายสถานะ **"รอตรวจสอบสลิป"** (สีคราม animate-pulse) และแทนที่ปุ่ม "ชำระเงิน" ด้วยป้าย "รอตรวจสอบ" ป้องกันการส่งสลิปซ้ำ
  - ใน `TenantInvoiceView.tsx`:
    - แสดงป้าย "รอตรวจสอบ" พร้อม icon หมุน เมื่อบิลอยู่ระหว่างตรวจสอบ
    - ป้องกันการเลือกบิลที่กำลังรอตรวจสอบสำหรับชำระเงินซ้ำ
  - ใน `TenantPaymentView.tsx`:
    - เพิ่มกล่องแจ้งเตือนเหตุผลการปฏิเสธเด่นชัดด้านบนก่อนส่วนเลือกช่องทางชำระเงิน และอ่านเหตุผลจาก `rejectedReason || rejectionReason`
    - รองรับการเลือกสลิปใหม่และกดส่งสลิปใหม่ (Re-submission) ได้อย่างราบรื่น
  - ใน `TenantWorkspace.tsx`:
    - เพิ่ม `'checking'` และ `'CHECKING'` ใน `allUnpaidBills`
    - กำหนด `activeUnpaidBill` ให้เลือกบิลที่กำลัง `checking` หรือ `rejected` ก่อนบิลทั่วไป เพื่อให้แสดงผลสถานะการตรวจสอบได้อย่างถูกต้อง
    - ตั้ง `onOpenPayment` ให้เปิด `subView: 'payment'` โดยตรงพร้อมเลือกบิลที่ถูกต้อง
- [x] Step 4: สร้าง Unit Test ชุดใหม่ `server/src/__tests__/unit/card-b4-slip-review-and-rejection-reason.test.ts` ทดสอบครบทั้ง 4 ACs (8 tests PASS)
- [x] Step 5: Gate A Review จาก Reviewer Subagent (**PASS**)
- [x] Step 6: รันการทดสอบ Unit tests (50 tests PASS across 7 files), build frontend & backend (ทั้งสองผ่าน exit code 0)
- [x] Step 7: Deploy ไปยัง Pilot server (`https://app.hor-plus.com`) และรัน Live Verification Scripts + ถ่าย Screenshot บันทึกหลักฐาน (ทุก AC PASS)
- [x] Step 8: Gate B Review จาก Reviewer Subagent (**PASS**) และทำ Git Commit ครั้งเดียว

## 5. Deploy & Command Log
- Deploy Timestamp: 2026-09-25T17:51:00+07:00
- Health Check:
  - `GET https://app.hor-plus.com/api/v1/health/readiness` -> HTTP 200 `{"status":"UP","database":"UP","redis":"UP"}`
- Commands Executed:
  - `npm --prefix server run test -- card-b4-slip-review-and-rejection-reason.test.ts` (exit 0, 8 tests PASS)
  - `npm --prefix server run test -- card-s1-tenant-identity.test.ts card-r1-tenant-richmenu.test.ts card-b1-three-bill-types.test.ts card-s6-public-policy-and-daily-stay.test.ts card-s7-upload-security-and-limits.test.ts card-r3-rejection-and-revision-diff.test.ts card-b4-slip-review-and-rejection-reason.test.ts` (exit 0, 50 tests PASS)
  - `npm --prefix server run build` (exit 0)
  - `npm run build` (exit 0)
  - `node scripts/agent/check-b4-live.mjs` (exit 0, all 4 ACs PASS)
  - `node scripts/agent/run-b4-live-browser.mjs` (exit 0, all screenshots captured)

## 6. Reviewer Log
- Gate A: **PASS** (Subagent `484fd32b-1368-45ee-91bb-7197293c725a` evaluated all 4 ACs, plan steps, REQ §7 compliance, and data isolation).
- Gate B: **PASS** (Subagent `61e51f85-9591-4264-80e3-9daa06d93fba` evaluated all 4 ACs with live screenshots and API evidence; non-blocking finding F-1 resolved).
